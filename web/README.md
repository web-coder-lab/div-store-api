# Div Store Web (preview / edit)

Open `index.html` in a browser, or:

```bash
cd div-store-web
python3 -m http.server 8787
# http://localhost:8787
```

API: https://div-store-api.onrender.com  
Change API in `index.html`: `window.DIV_API = '...'`

Tabs: Apps · Games · Research  
Submit, Studio, Install sheet included.
